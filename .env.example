# Copy to .env.local and fill in from Supabase → Project Settings → Data API / API Keys.
# Without these the app still runs, but everything stays on one device.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
